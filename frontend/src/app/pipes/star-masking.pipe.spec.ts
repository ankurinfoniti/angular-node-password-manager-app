import { StarMaskingPipe } from './star-masking.pipe';

describe('StarMaskingPipe', () => {
  it('create an instance', () => {
    const pipe = new StarMaskingPipe();
    expect(pipe).toBeTruthy();
  });
});
